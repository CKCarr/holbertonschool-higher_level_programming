#!/usr/bin/node
// main.js

// Require the add function from 13-add.js (Note: Provide the correct file path if needed)
const add = require('./13-add');

// Use the add function to perform addition
const sum = add(3, 5);
console.log(sum); // Output: 8
